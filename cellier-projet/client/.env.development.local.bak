# Reliable file watching in Docker/WSL dev containers
CHOKIDAR_USEPOLLING=true
WATCHPACK_POLLING=true

# HMR websocket when using forwarded port 3000 from the dev container
WDS_SOCKET_PORT=3000

# Keep ESLint feedback in the editor without slowing each webpack rebuild
ESLINT_NO_DEV_ERRORS=true
